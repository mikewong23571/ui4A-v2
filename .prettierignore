# 锁文件由包管理器维护
pnpm-lock.yaml

# 非代码工作流管辖的文档与脚手架产物(docs/ 为架构正典原样保留;conductor/ 文档另行维护)
docs/
conductor/
GOAL.md
DECISIONS.md

# create-next-app 脚手架附属文件
apps/web/README.md
apps/web/AGENTS.md
apps/web/CLAUDE.md
apps/web/public/

# E2E 场景 server 的独立构建目录
apps/web/.next-e2e/

# 发布时点产物(SHA256SUMS 与内容互相寻址,重排即失配;T22 release 资产)
release/

# Helm Go 模板(内嵌 {{- }} 指令,非纯 YAML,prettier 不可解析)
deploy/helm/
