Task: 前端 presence 上报器\n\nAdded a pure structured route observer and debounced reporter, with initial null suppression, per-kind coalescing, duplicate suppression, bounded protocol payloads, silent transport failure handling, and a same-origin POST transport. Wired a Suspense-safe client component into the root layout and kept a per-tab client instance id in sessionStorage.\n\nFiles: apps/web/src/presence/client.ts, apps/web/src/presence/client.test.ts, apps/web/src/components/presence-reporter.tsx, apps/web/src/app/layout.tsx.\nVerification: focused reporter tests (3/3) and web typecheck passed.\nWhy: presence should follow explicit URL protocol state without affecting browsing when the auxiliary signal is unavailable.
