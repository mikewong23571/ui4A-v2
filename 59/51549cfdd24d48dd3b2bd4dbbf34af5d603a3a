T31 Phase A Task 2: spec/plan 回改对齐 D48

变更:
- conductor/tracks/t31-quality-remediation_20260826/{spec,plan}.md 全文 D47→D48 引用修正(plan 头部/Phase A/Phase C/验收标准/spec 各发现项与验收方向);
- index.md 同步:D47→D48、状态 planned→in_progress、T27 冲突面解除说明;
- metadata.json status→in_progress、updated_at 刷新。

Why: 决策编号从 D47 校正为 D48 后,track 文档引用需一致;状态字段与 tracks registry [~] 对齐。编排 agent 自治验收:rg 确认 t31 文档无残留 D47 引用。

==== Phase A Verification & Checkpoint(自治验收,编排 agent 代行)====

验证命令与结果:
1. CI=true pnpm check
   - typecheck / eslint / governance(GR1-GR5)全绿;
   - vitest:384 测试文件通过、db 项目 81 文件通过;2 个文件共 41 例失败:
     apps/agent-runner/src/production.test.ts(/dev/shm 于 macOS 不存在,mkdir EPERM)
     与 scripts/t22/t22-compose-config-init.test.ts(非 root 无法 chown uid=1000)。
     两者均为 T22 生产形态测试的平台环境性既有失败(Linux-only 前提),与本
     Phase(纯文档)无关、先于 T31 存在,登记为宿主环境约束(darwin 开发机),
     不计入本 Track 回归口径;Phase E 验收将再次如实复述。
2. CI=true pnpm e2e invariants
   - 4 passed(I3/I4/I6/I5 全量重放 hash=e84eb0c3d2ec)/ 2 skipped(exit 0);
     skip 两项为现状登记的语义取代跳过。

环境备注:验证前临时 docker stop ui4a-images-web-1(发布 127.0.0.1:3100,与
Playwright 固定端口冲突;见 D5),运行后已立即 docker start 恢复。

本 Phase 变更文件全部为文档(DECISIONS.md、t31 spec/plan/index/metadata),
无代码变更,无需新增测试。Phase A 完成。
