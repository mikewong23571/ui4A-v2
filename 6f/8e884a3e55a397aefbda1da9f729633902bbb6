Task: 上报端点 POST /api/presence\n\nAdded the authenticated POST endpoint. It parses the closed presence contract before any side effect, resolves the trusted principal with ui4a:write, rejects ungranted declared scopes, initializes the independent projection, and returns idempotent append results or bounded-rate/auth errors. Presence writes bypass business judge/guards/effects by design. The events audit endpoint now accepts the presence domain.\n\nFiles: apps/web/src/app/api/presence/route.ts, apps/web/src/app/api/presence/route.test.ts, apps/web/src/app/api/events/route.ts.\nWhy: make all four attention change points reportable while keeping identity and authorization server-owned.
