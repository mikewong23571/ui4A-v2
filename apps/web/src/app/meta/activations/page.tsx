'use client';
/**
 * BIOS 激活队列面:/meta/activations → meta/activations(pending 清单)。
 */
import { ActivationsQueueBody } from '@/components/meta/meta-lists';

export default function MetaActivationsPage() {
  return <ActivationsQueueBody />;
}
