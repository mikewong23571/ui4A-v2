'use client';
/**
 * BIOS 定义查看面(列表):meta/flows(浏览器路由 /meta/flows;数据 /_meta/api)。
 */
import { FlowsListBody } from '@/components/meta/meta-lists';

export default function MetaFlowsPage() {
  return <FlowsListBody />;
}
