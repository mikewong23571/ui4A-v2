Task: Production Agent Runtime activity wiring Red\n\nSummary:\n- Proved the existing Agent Run workflow calls specialization execute locally inside Worker and never consumes runtime-backends production composition.\n- Defined a replacement execute-only seam: Worker compiles generic transport, server resolves profile/backend, Runner executes transport, specialization accepts result; Temporal collect/verify/finalize remain unchanged.\n- Covers production source wiring, override rejection, selected Host unavailable with no K8s/local fallback, and cross-run result rejection.\n- Red: 9/9 failed on missing production-wiring.ts and absent activities import; no DB/network/Codex.\n\nFile:\n- apps/worker/src/runtime-backends/production-wiring.test.ts
