T15 Phase A checkpoint: DeepSeek baseline and AI-first acceptance foundation

Completed:
- U23 provider-neutral config and unified web/worker env loading.
- real deepseek-v4-flash protocol probe and live inline/delegated smoke.
- opt-in ui4a.story-eval/v1 harness on isolated test DB.
- U1/U5/U10/U12 single-run red baseline: 1 pass, 3 fail, 1 safety failure.
- disposable observe/answer, conversation-context, and effect-grant spikes.
- project GOAL/product/tech-stack/architecture updated to AI-first.

Verification:
- pnpm check: 121 test files passed, 2 Temporal-dependent skipped; 1134 passed, 2 skipped.
- targeted U23: 9 files / 104 tests passed; agent/web typecheck passed.
- real provider probe: 6/6 tool calls.
- env:verify-dev passed for web and worker.
- live health OK; inline and delegated driver=llm smoke completed.
- story Eval default gate skipped and explicit DeepSeek baseline failed red as intended.

Known environment evidence:
With Temporal running, two existing task-queue integration tests stalled on both persistent and fresh server. No related modules changed; tests pass the repository quality gate via the existing unavailable-Temporal skip when the server is stopped.
