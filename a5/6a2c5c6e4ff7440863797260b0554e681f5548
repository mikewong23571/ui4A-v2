Correction: extracted the database-facing Chat situation adapter from the large route so the route remains within the GR3 size baseline. The adapter loads optional principal presence, normalizes policy scopes, falls back silently to explicit/default structured context, and delegates all selection to assembleSituation. Added a headless adapter test; web typecheck and focused Chat/situation tests passed.
