# UI4A 深度体验审查（以人为本 + UI 设计双视角）

> **本轮交付（2026-09-05）**：已完成 20 条主故事与 5 条回归/延伸场景，八类修复已部署到 `c9971648`。
> 最终全量门禁 `4046 passed / 15 skipped`，八服务 healthy。新建实验应用已停用、待确认队列为0。
> **仍有未修复边界**：严格策略下 Meta 挂起请求的批准路径未贯通；本轮通过驳回旧请求、在恢复后的默认策略下重新停用完成收尾。停用后403被误呈现为服务故障及知情回执问题仍在。
> 下方第一轮记录保留为历史；当前结论以第二轮逐步证据与文末结案表为准。
> **后续修复入口**：[剩余缺口与候选修复方案](docs/ux-review/2026-09-05/remaining-gaps.md)按G01–G15列出证据、根因/待验证假设、最小方案、边界和验收条件。

- 环境：`https://ui4a.styleofwong.cn`（release `bb718533`，v0.1.0-experimental.1）
- 账号：mike（真实 PKCE 登录）
- 日期：2026-09-05
- 方法：浏览器黑盒深度使用，至少 20 个用户故事，覆盖聊天、业务 CRUD、Meta 治理、会话面板、账户等；每个故事同时记录「以人为本交互」与「UI 视觉设计」两类发现。
- 标记：🔴 违背第一性原理/阻碍知情决策；🟡 违背直觉/不流畅；🔵 视觉设计问题；✅ 做得好的地方

## 发现记录（持续更新）

### A. 交互 · 以人为本

1. 🔴 **确认卡片无法支持知情决策**（首页「在等我」/ canvas 确认详情）：卡片标题只有动作名「archive · 由 agent 提议」（英文动词直出 + 机翻腔），不显示被操作对象的人类名（实际是归档待办「预约体检」），也不显示任何 payload 摘要。批准人必须点开 `todo:item-2` 裸链接跳到另一个页面才能猜到要批什么。第一性原理：确认 = 让人在知情下决策；当前设计要么逼人盲批，要么逼人多跳两页。
2. 🔴 **决策后无回执**：批准/驳回执行后，详情页标题退化为裸内部 ID（`confirmation:c1`/`c2`），动作区直接消失，没有「已批准/已驳回（理由：…）」的落账回执。驳回时认真填写的必填理由在任何页面上都看不到。人做完决定后得不到任何反馈，信任感受损。
3. 🟡 **创建工作线表单是工程视角而非人的视角**：字段顺序把技术 slug（工作线标识，要求小写字母+横线）放在第一位，而人自然先想「目标」；「目标来源」要求手填 `chat:m41` / `review-0712` 这类内部引用格式，系统明知会话与事件却提供不了来源选择器。
4. 🟡 **产品自造词门槛高**：全局导航与页面上大量自造术语——「共同注视」「定义站」「声明的处境」「在哪」「工作线」「原始合同」。新用户没有任何线索知道这些词对应什么任务。「在哪」按钮尤其费解（实际是 lens 定位/处境披露）。
5. 🟡 **实体属性表暴露内部字段**：`rel` / `flow` / `identity` 三行技术字段与业务字段（状态/标题/备注）混排同一张表，普通用户无法区分哪些与自己有关。
6. 🔴 **核心聊天/Agent 路径在生产不可用**：全新会话发送任意消息（含「现在有哪些应用？」这种极短请求）一律失败——`driver_fail: provider request UTF-8 JSON is 36,809~37,305 bytes; limit is 32,768 bytes`。说明静态上下文装配（系统提示+站点清单+实体快照）本身已超过模型请求上限，且随事件日志增长持续变大。产品的「AI-first」主循环当前 100% 失败，只能靠人工 entity 动作兜底。（两次字节数不同：36,809 → 37,305，证明上下文随事件流增长。）
7. 🟡 **失败反馈缺少恢复指引**：报错只给出字节工程细节，没有任何「下一步怎么办」；两次同样的失败文案措辞还不一致（疑似 LLM 改写错误文案），且无「重试」「复制错误」「联系入口」。
8. 🟡 **「遇到问题」= 裸事件流**：失败消息旁的「遇到问题」链接直接打开 `/api/events?afterSeq=403` 原始 JSON。审计透明是好事，但把它当作用户的错误恢复入口违背直觉。
9. 🟡 **聊天头部暴露裸会话 ID**（`UI4A 助手会话 3cd5760a`）；头部五个纯图标按钮（新会话/历史会话/悬浮/独立窗口/收起）无可见文字，含义只能悬停猜测。
10. 🟡 **「添加涉及对象」表单自相矛盾**：帮助文案写「通常经『＋添加涉及对象』的选择器挑选，无需手填」，但表单只提供裸文本框，没有任何选择器；「类别」下拉直接暴露合同英文词（context/active/approval/event）无中文解释。
11. 🟡 **工作集状态标签中英混排**：中文界面里对象状态显示英文小标签 `archived`/`open`/`pending`。
12. 🟡 **中文文案使用半角标点**：「说出你想做成的事,如「发布一篇文章」;助手与你过同一道门,每步可见。」等大量文案用半角逗号/冒号/分号，与全角标点混用，排版气质割裂。
13. 🟡 **「委托模式： 协同」无法理解**：聊天输入框旁的模式切换按钮，没有解释「协同」与另一模式（委托？）的差异和后果。
14. 🔴 **想法工作区主动作「再记一条」点击无反应**（两次复现）：ideas 工作区捕捉卡片上的「再记一条」按钮点击后不出现表单、不导航、无任何反馈；用户只能通过页面底部小链接「想法捕捉」进入 flow 实体页才能捕捉——应用主入口的行动回路断裂。
15. 🟡 **flow 实例身份错位**：`flow:idea-capture` 实体页的标题/identity 显示的是上一次捕捉的想法名（如「周末去爬山」），但它实际是「等待下一次捕捉」的流程实例；想捕捉新想法的用户会误以为这页是旧想法的详情。
16. 🟡 **捕捉/发展等动作成功后无页面级成功反馈**：表单提交后直接收起，无 toast/成功态（仅 entity 动作区偶发一行 status「已执行…」，且转瞬即被后续渲染覆盖）；对比失败时也安静，用户无法区分成功与失败。
17. 🟡 **「开始发展」直接执行无确认也无输入**：与「归档」有危险样式+两步确认形成不一致的保护梯度；发展这个关键状态跃迁过于轻易。
18. 🟡 **实体属性表混入英文裸字段名**：发布向导 flow 实体属性显示 `title` / `category` / `tags` / `body` 英文键名，与「状态」等中文行名混排；其他应用（todo/ideas）则是中文行名——同类信息两种呈现。
19. 🟡 **必填标记不可信**：发布步骤表单「文章标题 *」标注必填，留空提交仍然成功发布——要么校验缺失，要么必填标记撒谎。
20. 🟡 **向导步骤指示器不更新（工作区内嵌卡片）**：发布向导在应用工作区里推进到「分类」「正文」后，内嵌卡片的步骤emphasis仍停留在「基本信息」，用户无法从工作区感知当前走到哪一步，只有进入 flow 实体页才能看到真实状态。
21. 🔴 **Draft 审批在无 lens 上下文时必然失败且报错费解**：直接打开 `/meta/entity?rel=draft:*`（无 scope）点 Approve → 两步确认后服务端拒绝：`[schema-invalid] Draft action requires an explicit authorized application lens`。用户事先得不到任何提示（页面不显示当前 lens、不引导先选视角），只有提交后才报英文技术错。带 `?scope=governance` 进入后同一操作即成功。UI 应在入口声明 lens 或提前拦截。
22. 🟡 **治理 receipt 仍是转义 JSON**：Approve 成功后的执行结果把 checks/submissionPolicy/evidence 显示为转义 JSON 字符串（`"[{\"name\":...}]"`），人类可读性差；对比「激活可见性披露」部分则是结构化中文（好）。
23. 🟡 **披露与目录名称不一致**：激活披露说「新装应用： t51-prod-2694」，应用目录里同一应用显示名是「T51 Prod Walkthrough」，无 slug 对照，用户难以对上号。
24. 🟡 **owner/UUID/UTC 原样暴露**：草稿页 owner 显示裸 UUID，expires 显示 UTC ISO 串（2026-10-04T14:38:15.577Z），未本地化、未给出人类可读表述。
25. 🟡 **Meta 平面语言混杂**：区块标题 Validation / Mechanical diff / Evaluation / Sources & provenance 英文，说明文字中文；按钮 Approve/Reject 英文（业务平面同义按钮是 批准/驳回 中文）；「审查责任点」区块中「当前合同未声明可操作动作」与紧随其后的可用动作按钮自相矛盾的文案并存。
26. 🟡 **工作集出现「对象不存在」悬挂引用**：工作线工作集自动挂入了两个 `message:UUID` 条目（发起聊天时自动关联），但渲染为「对象不存在」+ 裸 UUID——数据被引用却无法解引用，且没有任何解释或清理指引。
27. 🟡 **系统菜单「收件箱」是通用实体视图**：期待一个收件箱消息列表，实际跳到 `/entity?rel=inbox` 的属性表（rel/count/delivered），空态也以字段表呈现——菜单标签承诺了一个人造页面，交付的是合同投影。
28. 🔵 **事件流时间线被 presence 遥测刷屏**：17 条中 10 条是 `presence-site/focus-changed`（浏览行为本身产生的事件），且无按 kind 过滤；真实业务事件（draft-accepted、执行「publish」）被淹没。「机械摘要+零 AI」的诚实性很好，但信噪比不适合人读。
29. ✅ **退出登录正确结束 SSO**：经 OIDC RP-Initiated Logout 回到 Keycloak 登录页（非静默续登），重新登录后授权完整恢复。
30. ✅ **「刷新授权」SSO 往返顺畅**：/session → Keycloak → 回 /session，无需输密码（SSO 会话内）。
31. ✅ **404 诚实呈现**：不存在的实体显示「实体 "todo:does-not-exist" 不存在(404)。」并提供返回首页出口。
32. ✅ **历史会话可恢复**：会话列表（含诚实的「失败」标记、本地化时间）点击即恢复完整上下文。
33. 🟡 **历史会话预览文本无截断**：列表项直接展示 agent 长输出整段文本，应截断为标题/首条用户消息。

### B. UI 视觉设计

14. 🔵 **Canvas/工作线页应用书架按钮截断成单字**：thread 视图下 10 个应用按钮被压成「社…」「软…」「T…」，名称完全不可读（首页则显示完整名称），同组件两种极限表现，缺宽度自适应策略。
15. 🔵 **应用书架横向溢出**：首页书架 10 个应用按钮单行排列，后两个名称被截断（"T48 Produ…"、"T51 Prod …"）且没有省略提示或换行策略，视觉上像被裁掉。
16. 🔵 **确认卡片动作按钮排版松散**：批准（红色危险样式）与驳回之间用大段虚线分隔，两行按钮占的纵向空间几乎等于卡片内容本身；卡片内同时存在链接、状态行、标语、按钮组，视觉层级不分明。
17. 🔵 **实体页标题区噪声**：h1 业务名下方直接裸排 `todo:item-2` 机读 ID，与「查看原始合同」按钮挤在一起，既非内容也非动作，位置尴尬。
18. 🔵 **整页截图出现内容重复伪影**（fullPage 截图中页头+书架+我的事渲染两遍；DOM 只有一份）— 初步判断为截图拼接伪影而非页面 bug，待后续 viewport 截图复核。
19. 🔵 **Canvas 主区大面积留白**：工作线详情页内容集中在上半屏，中部以下全部空置，布局重心失衡（与左侧书桌、右侧聊天三栏并存时更明显）。

### C. 做得好的

10. ✅ **批准/驳回均为两步确认**（先请求 → 「确认并执行」），防误触设计一致。
11. ✅ **驳回要求必填的结构化理由**（reason *），拒绝是事件不是异常。
12. ✅ **数据一致性**：两条确认处理完后首页「在等我」与 inbox 实体的 count 同步归零；todo 状态正确变为「已归档」，归档后动作按钮正确消失。
13. ✅ 未登录访问 UI 页 307 → Keycloak、API 结构化 401 的边界行为符合合同。

## 用户故事进度

| #   | 故事                                                     | 结果                                           |
| --- | -------------------------------------------------------- | ---------------------------------------------- |
| 1   | 匿名访问 → 307 → Keycloak PKCE 登录                      | ✅                                             |
| 2   | 首页「在等我」确认列表浏览                               | ✅                                             |
| 3   | 确认 c1 详情 → 两步批准 → todo 归档生效                  | ✅                                             |
| 4   | 确认 c2 → 必填理由驳回 → inbox 归零                      | ✅                                             |
| 5   | 实体页浏览（todo:item-2 / inbox）                        | ✅                                             |
| 6   | 创建工作线（ux-review-2026）                             | ✅                                             |
| 7   | 工作线添加涉及对象（类别+裸路径）                        | ✅                                             |
| 8   | 聊天窗真实 LLM 对话（新建会话 ×2）                       | ❌ 核心 bug：上下文超限 100% 失败              |
| 9   | 应用目录 /applications 浏览                              | ✅                                             |
| 10  | 想法收集全生命周期：捕捉→发展（含内容）→成熟             | ✅（工作区「再记一条」断裂，走 flow 实体路径） |
| 11  | 待办事项应用 CRUD（捕捉→完成→重开/归档）                 | ✅                                             |
| 12  | 内容发布三步向导（起草→分类→正文→发布）                  | ✅                                             |
| 13  | /meta 定义控制台浏览（视角/筛选/资产入口）               | ✅                                             |
| 14  | Draft 治理：草稿列表→详情→两步 Approve→激活披露→目录验证 | ✅（无 lens 时 Approve 被拒=bug #21）          |
| 15  | Meta 应用定义页（T52 声明动作渲染，只读披露）            | ✅                                             |
| 16  | 专职 Agent 定义浏览（4 个）                              | ✅                                             |
| 17  | /session 我的授权面板（12 应用+治理展开说明）            | ✅                                             |
| 18  | 刷新授权（SSO 往返）                                     | ✅                                             |
| 19  | 账户与密码（Keycloak Account Console）                   | ✅                                             |
| 20  | 退出登录（RP-Initiated Logout 结束 SSO）                 | ✅                                             |
| 21  | 重新登录恢复                                             | ✅                                             |
| 22  | 聊天历史会话面板与恢复                                   | ✅                                             |
| 23  | 「在哪」处境披露弹层                                     | ✅（文案连读费解）                             |
| 24  | 事件流审计时间线                                         | ✅（presence 噪声多）                          |
| 25  | 委托监控                                                 | ✅                                             |
| 26  | 收件箱入口                                               | ✅（通用视图客串）                             |
| 27  | 不存在实体 404 错误路径                                  | ✅                                             |
| 28  | 工作线完整生命周期（创建→挂对象→暂停→恢复→完成）         | ✅                                             |
| 29  | 聊天修复后真实 LLM 闭环复验                              | ✅ 修复已部署，真实回答+依据引用               |

## 关键阻塞修复记录

### 🔴→✅ 阻塞 #6：聊天/Agent 主路径 100% 失败（已修复并部署）

- **根因**：`packages/agent/src/llm/llm-driver.ts` 硬编码自设预算 32KiB（D54.4），非提供商限制；生产装配体（系统提示+工具+含 sitemap 披露切片的 user prompt）在全新会话即 36.8–37.3KB，随事件日志增长。
- **修复**（commit `fe58641e`，D72）：预算解析改为 显式覆盖 > `LLM_PROVIDER_REQUEST_BUDGET_BYTES` 环境变量 > 默认 512KiB；诚实失败语义不变；测试与默认值解耦；`.env.example`/`DECISIONS.md` 同步。packages/agent 310 个单测全绿，governance 通过。
- **部署**：home 构建三镜像（fe58641e），push 本地 registry，compose-inputs 更新 digest，preflight/up/status 全绿，8 服务 healthy，volume hash 不变；浏览器真实聊天复验通过。
- **部署文档**：DEPLOYMENT.local.md §5/§12 已更新并同步 home，两份 SHA-256 一致（`9f32dfcd…`）。

### 历史阻塞：Draft 审批无 lens 时必失败（发现 #21；第二轮已修复）

当前结论：R16已通过显式应用视角选择修复浏览器入口，不再采用下面历史建议中的自动默认lens。严格Meta确认批准未完成的问题另见G01。

- 直接打开 `/meta/entity?rel=draft:*` 无 scope 时，Approve/Reject 提交被服务端拒绝：`[schema-invalid] Draft action requires an explicit authorized application lens`。带 `?scope=governance` 进入后成功。
- 历史聊天记录显示 2026-09-04 的 CLI agent 通道曾因同一错误七次创建 Draft 失败（证据在 docs/CAPABILITY-GAPS.md）——人机双通道、两个平面（web meta 控制台 + CLI）同源问题。
- 建议修复方向：meta 控制台在草稿页入口声明当前 lens（或默认带 authorized-apps lens），无 lens 时提前拦截并给中文指引，而非提交后报英文技术错。

## 第二轮：2026-09-05 当前部署深度复验（本轮完成）

本轮独立从浏览器真实 PKCE 登录开始；不将上一轮记录视为本轮完成证据。
起始 `/live` 与 home checkout 均为 `1bf3dd29956426c44d3137095e1a02a52a6d987b`，八服务 healthy。
浏览器 session 为 `ux0905`，数据前缀 `UX0905`。依据北极星的注意力成本、共同上下文、合同驱动、知情决策审查。
用户已授权体验数据增删改查与必要简单修复；凭证不进入文档，系统治理仍经过当前声明动作和确认门。

### 本轮步骤与证据（随操作追加）

- R01 登录并恢复工作台：公网 → Keycloak → mike 密码登录 → 我的事；没有待办确认，旧工作线「完成一轮以人为本的产品体验审查」显示已完成。发现首页仍展示 completed + 裸 thread id，给人的工作信息被技术身份稀释。

### 修复计划与判定

先以真实浏览器验证旧发现的当前性；可由读码确定的机械缺陷采用小改动；不确定的运行行为先做临时 probe，再补回归验证。优先闭环主动作不响应与 Draft 无 lens 的错误到达。其余结构性设计问题保留明确证据与后续范围，不把“多加确认”当成体验改善。

- R02 想法工作区继续捕捉（阻塞复现）：点击「再记一条」后按钮短暂 disabled 随即恢复，标题与「已记下」不变；点击「重新载入」仍旧。只读浏览器 fetch 同门合同确认 `flow:idea-capture` 实际已变 `node=capture`、action=`keep`；沿「想法捕捉」进入实体页，正确出现「记下想法」。根因收窄至页面别名缓存：workspace 以 `flow:idea-capture` 取数，提交使用 `idea-capture:main`，缓存只删 canonical key。旧 A14 应修订为“操作成功但投影陈旧”，不是动作没执行；同源解释旧 A20 向导步骤不更新。

- R03 捕捉并发展新想法：创建 `idea:ux0905`「UX0905 减少评审验证税」→集合真实计数 3→开始发展→提交内容「确认卡同时展示对象名称、改变前后与依据…」。实体回读为发展中且正文一致。新阻塞 F-R03：同节点再次编辑按钮永久 disabled，浏览器整页刷新才恢复；组件测试已红，原因是 ActionRunner 的 executed 临时状态在属性更新后不重置。
- R04 无视角起草应用：浏览器填入完整 `ux0905-review-lab` bundle、点击 Create Draft，出现 `Draft action requires an explicit authorized application lens`。页面没有就地选择应用视角的入口，用户在填完 JSON 后才知道前置条件。保留服务端 D65/D66 约束，计划在 Draft 动作前提供基于授权 sitemap 的显式视角选择，不暗选首项、不把视角当权限。

- R05 当前对象只读协作与引用回到事实：在 `idea:ux0905` 问“只根据我正在看的这个想法，概括验收标准，并指出当前还缺什么信息。不要修改数据。”真实 LLM 正确概括「不跳页即可知情决定」，提出量化口径/信息边界/依据来源等缺项，带四个字段引用。点击正文引用进入共同注视同一想法，正文与动作一致；此前跨页进入 Meta 后再回来，回答仍从历史恢复。注意力缺陷：回复前仍显示「导航到 ideas」机械行，引用标签为 JSON Pointer，工作台标题重复。
- R06 AI 执行精确对象状态变更：在正文引用落面后说“把这个想法标记成熟。只操作当前这一个。”助手读取当前想法→执行「标记成熟」→画布实时变已成熟，仅剩归档动作，提供执行依据。用户不必重复对象 ID，符合共同上下文；但简单单动作要四步且有两次同对象读取，时延与轨迹占用注意力仍偏高。
- R07 AI 高风险归档→人类批准→事实验收：同会话请求“把这个想法归档，保留记录。只归档当前这个想法。”助手产生 `confirmation:c3`，想法仍保持成熟，未提前变更；回首页在等我看到确认→进入详情→两步批准→想法归档。旧 A1/A2 再现：卡片标题只有 archive、对象名不见，批准后退化为 `confirmation:c3`，没有清晰已批准回执。完成依靠回对象核实，验证税未消除。

修复提交 `93239b7f`：页面缓存根据已观察到的 canonical rel 失效别名，旧飞行请求不回填；实体 fields 更新后重新开放可重复编辑动作；Draft/草稿激活无 scope 时先显式选择授权 sitemap 中的应用，再进入原页面动作。全量 `pnpm check`：4038 passed / 15 skipped；Meta/动作/缓存相关 149 passed，最后激活补充后的 focused 3 passed。当前正在 home 构建 exact SHA 三镜像，尚未把本地测试当作线上修复完成。

- R08 建立跨应用工作上下文：创建 `thread:ux0905-deep-review`「UX0905 完成跨应用评审闭环」，在首页通用表单挂入 `idea:ux0905`；进入工作线后「＋ 添加涉及对象」确有真正选择器，按标题过滤“体验审查”后挂入发布应用文章，再通过对象旁「移出」移除。工作集 1→2→1，原文章不被删除，重复添加按钮明确 disabled。修正旧 A10：缺选择器仅是首页通用动作路径；工作线书桌存在可用选择器，同一任务两种体验不一致，宜收敛。评论候选全部叫“评论审核”无法区分，是选择器标题信息不足。
- R09 工作线收尾与移动端：暂停→恢复→完成→两步归档，状态与动作均同步，工作集记录保留。390×844 实测出现水平溢出（document scrollWidth=656，viewport=390），第一屏几乎只剩书桌，操作区被挤出可见范围；这不是截图拼接，是真实布局缺陷。截图 `r09-thread-mobile.png`。工作线状态中文，但成员 archived 英文仍混排。
- R10 发现并处理待办：目录搜索“待办”→进入应用→捕捉 `todo:ux0905`「UX0905 验证发布后操作回执」带备注→列表计数 4→完成→重新打开。实体只暴露状态变更，没有修改标题/备注动作，这是合同能力缺口。已完成状态只提供重新打开，想归档必须先重新打开，增加无意义状态往返。
- R11 人类归档取消与继续：首次请求归档后点取消，状态仍进行中；再次请求并确认时收到 `[http-202] 未知错误`，实际服务端已挂起确认。核心阻塞 F-R11：业务 exec 客户端只识别 entity 成功体，把标准 202 suspended 归入未知错误；应保留待确认状态并给声明确认实体入口，不能宣称归档已执行。
- R12 带理由驳回保留业务对象：从首页在等我找到 human 提议的归档，填写“UX0905：验证拒绝路径，保留待办等待修复后再归档。”→两步驳回→确认从首页消失，待办保留。后续恢复将直接使用新发布的待确认链接。驳回理由在首页未留回执，仍需审计验证。
- R13 发布向导必填失败恢复（进行中）：在应用工作区新向导不填标题点击下一步，被前端校验阻止，显示 `must have required property '文章标题'`；填入「UX0905 深度体验发布记录」后继续。旧 A19“空标题仍发布”本轮未复现，不应继续当作确诊缺陷；英文错误摘要+字段错误重复出现仍增加噪声。
- R13 完成：标题必填失败恢复→分类 review、标签 ux0905→正文→发布前改名「UX0905 深度体验发布记录（复核版）」→文章列表 count=4，新文章内容/分类/标题一致。发布后向导直接复位，无新文章直达回执，用户必须自己找集合。此前工作区步骤不动由同一别名缓存复现，发布后将再次无刷新验收。
- R14 已发布内容的可逆下线：新文章 `post:ux0905` 下线→状态已下线且正文保留→重新发布→再次下线，验证动作随状态变化，旧文章不受影响。最终测试文章保留为已下线。产品缺口：已发布内容无编辑动作，写错正文只能依赖新建；完成/下线状态都缺归档动作，与用户“收尾”心智不一致。集合成员把整段正文作为链接文字，阅读密度过高；自动点击长链接中心落在换行空隙，本轮用已观察 href 导航，未将该工具点击现象误判为后端错误。
- R15 不完整系统提案→修复候选：在 governance 视角创建 `draft:71cb15c56f5236491e0d`，故意缺少 applications/capabilities/flows/seed；Draft 正确为 invalid，列出四个字段问题，不安装任何应用。通过 Revise Draft 补完整 bundle：新应用「UX0905 评审实验室」、声明保存/完成/重新评审动作、初始评审实体。版本 v1→v2，ready，三个 checks 通过且 diff 只有本次 app/flow。证实系统规则可由数据治理生长，无需前端特判。文案仍混杂英文，顶栏草稿处境显示“无法读取”，与正文正常形成矛盾。
- R16 从无视角深链接恢复并激活：提交 v2 待审批后，新版无 scope 深链接正确不再暴露必失败的 Approve，而显示未默认选择的授权应用列表；显式选 governance→到该 Draft 激活责任点→两步 Approve→披露“已对当前会话可见”。新应用从数据安装成功，修复 F-R04 线上闭环。新发现：新生成的激活页却写“本激活无 diff 载荷(diff 字段引入前的旧日志)”，属未经事实支持的旧数据推断；同时 accepted receipt 的重新计算 application-not-installed=false 看似失败，与激活成功混淆。两项影响知情理解，列为高优先后续治理呈现问题。
- R17 新生应用实际编辑与闭环（发现引擎阻塞）：目录立即进入新应用→保存标题与结论→不用刷新连续保存第二版，标题实时更新，跨应用证明缓存修复生效。工作区编辑表单未预填现值（实体页另行复验）。点击无需参数的“完成评审”却被拒“参数不符合动作字段 schema”。只读合同显示 complete 的 properties/required 均空，定义声明 collect-node-fields=false；引擎 judge 无条件合并 node.fields，与 Siren 投影分叉。F-R17 属同一合同失真，优先修复，不能用填假参数绕过已披露合同。
- R18 捕捉/连续编辑修复回归：新版想法工作区点击“再记一条”后原位动作立即变为“记下想法”，无需刷新；创建 `idea:ux0905-2`「UX0905 连续编辑回归」后列表与捕捉区同时更新。开始发展→画布保存第一版→实体页预填第一版→保存第二版→立即再次展开编辑且预填第二版。F-R02/F-R03 两处线上通过；同一机制还在新应用双次保存通过。实体页回填正确，工作区表单不预填现值仍作为独立 P2 差异保留。
- R19 待确认恢复路径复验：新版在 `todo:ux0905` 归档的二次确认后，显示“操作尚未执行，等待确认”与“查看待确认事项”，归档按钮锁定以避免重复提议；点击直达 `confirmation:c5`→两步批准→原待办已归档，备注保留。F-R11 已闭环。动作确未生效与请求已记录两件事清楚区分，未绕过人工门。
- R20 窄屏工作线复验：390×844 同一工作线页 now scrollWidth=390（修复前 656），书桌、说明与应用入口纵向可见且无需横向拖动。截图 r20-thread-mobile-fixed.png 与修复前 r09-thread-mobile.png 对照通过。应用入口仍限制九个并给全部应用出口，长标题有省略，但基本可读；F-R09 已闭环。
- R21 新应用授权与会话恢复：/session 显示凭证治理展开、已授权集合包含 ux0905-review-lab，点击刷新授权经 SSO 回同一 /session，无需再次输入密码。之后从系统菜单退出登录，验证真正结束 SSO并重新登录（后续记录）。面板仍展示 UUID 与机器 scope，作为授权审计入口可以接受，但目录应继续使用人类名称。
- R21 完成：退出回到真实密码登录页；安全重登 mike 后恢复首页和授权，未静默续登。
- R22 历史会话与新会话隔离（进行中）：重登后历史 c92ab766 三回合仍在，点击恢复可见原提问与引用；点击新会话清空对话，再问“现在还有哪些事项在等待我批准？…不要修改数据”。发现历史预览仍把已批准的归档回合标为“待确认”，它是旧回合终态而非最新业务状态，应明确标签语义；旧长会话预览无截断亦复现。
- R22 完成：新会话 ee4dc28c 读取 inbox，回答“当前没有任何事项在等待你批准”，与首页空态一致，未错误继承旧会话待确认状态，且零写入。回答仍引用 count/entities/actions 原始字段并解释三遍同一空态，AI 工作话语仍有压缩空间。
- R17 最终闭环（release 256645fe）：同一个已安装定义、同一个第二版评审实体，不改 payload、不补不该有的参数，点击完成评审即变已评审并只显示重新评审；重新评审→再次完成均成功。judge 与 Siren 共用 actionParametersSchema；false/true/缺省三种声明行为与额外参数拒绝都有回归锚。全量 check 4043 passed / 15 skipped，线上截图 r17-contract-fixed.png。F-R17 已修复并部署。
- R23 应用停用收尾（发现同源剩余路径）：在新应用定义页填写停用原因→两步确认，Meta client 仍显示 `[http-202] 未知错误`，目录仍可见；原因与 F-R11 相同但发生于独立 Meta HTTP 客户端。本轮不能宣称“待确认修复覆盖全站”，需让两个客户端共用 202 解析再复验停用确认链。只操作本轮新建实验应用，不清理历史其他应用。

R23 根因补充（部署站与代码双证据）：

1. `docker exec ui4a-web-1` 证实 `/app/apps/web/src/domain/policy.cedar` 缺失。审计中的归档确认均记录 `cedar:fail-closed`，“策略文件缺失或为空”。仓库既有策略应允许 human 经界面两步确认后直通，agent+high 仍挂起；镜像未带文件导致所有高风险人类动作额外确认。这是打包缺陷，不是应增加的责任环节。
2. Meta 停用确实生成 pending，但确认目标为 `meta/application:ux0905-review-lab`，businessApplications 递归无法从生命周期伪流定位归属，变为 unresolved；因此 inbox、确认链接和审计都过滤掉了它。修复让 confirmation 目标复用 metaApplications 同一归属解析，授予外仍拒绝，business 直访 meta 仍拒绝。
3. Business/Meta 两个客户端改共用 suspendedExecResult；停止把 202当失败或宣称已执行。

最小补救：镜像显式复制既有策略文本（不改规则）、复用归属解析、共用待确认响应解析。针对三条缺陷先红后绿；策略矩阵/镜像/受众 49 测试通过，客户端/表单 25 通过，正在全量门禁。没有把权限检查改成关注视角，也没有跳过 human-only。

### 本轮第一性原理判断

- **操作闭环优先于表面美化**：旧“按钮没响应”实为写已成功、读投影陈旧，重试可能产生重复副作用。修复缓存身份与回执，比增加 loading 动画更根本。
- **合同必须可执行**：Siren 说无参数，judge 却要求参数，是人机共同入口失真；统一生成函数与正反例测试，禁止在 UI偷偷补参数。
- **责任点应可到达且数量必要**：默认策略文件丢失造成额外人工确认；Meta 确认不可见又迫使人找机器日志。先修复部署事实与目标归属，不把多次点头误认为更安全。
- **显式应用视角不是权限**：草稿起草/审批前可选当前授权应用，未暗选 governance，也未扩展凭证权限。跨平面确认归属复用现有授权事实。
- **新增应用无需定制前端**：本轮新评审应用从 JSON定义、invalid→ready→批准→连续编辑→状态完成运行；最小修复全部在共同机制上，对想法、待办、发布向导与新应用共同生效。

### 仍需后续迭代的体验债（不混同已修复阻塞）

| 优先级 | 证据                                                                            | 改善目标                                                    |
| ------ | ------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| P1     | R07/R12 确认卡只有动作名，决定后只剩 ID；理由不见                               | 对象名称、变更摘要、已批准/已驳回与理由同面可见             |
| P1     | R16 全新激活误称“旧日志”，accepted后 check 显示 application-not-installed=false | 区分批准时检查与批准后当前事实，删除无依据时间推断          |
| P2     | R10/R14 已完成或已下线对象缺归档/编辑入口                                       | 通过应用定义治理补齐符合实际工作的动作，而非前端特判        |
| P2     | R08 首页裸 rel 表单与书桌选择器不一致；评论候选同名                             | 收敛到可发现、可区分的对象选择入口                          |
| P2     | R05/R06/R22 对话输出 schema/JSON Pointer、重复读取和冗长空态解释                | 让真实 LLM 以任务和可验证引用回答，优化披露，不写关键词路由 |
| P2     | R15/R16 Meta 中英混排、UUID/UTC、重复机械回执、顶栏“无法读取”                   | 让人先读目标、责任、差异；机制细节退到审计                  |
| P2     | R17/R18 工作区编辑不预填，实体页预填正确                                        | 共用 fresh entity 数据供表单，避免再次抄写                  |
| P2     | R22 历史会话“待确认”是旧回合终态                                                | 标明历史状态，或联动当前确认事实，避免误以为仍需审批        |
| P2     | R02/R18 捕捉器继续显示上一次产物名                                              | 区分流程位置与产物身份，避免旧标题误导新建                  |

视觉对照：[窄屏修复前](docs/ux-review/2026-09-05/r09-thread-mobile.png) / [修复后](docs/ux-review/2026-09-05/r20-thread-mobile-fixed.png)；[待确认回执](docs/ux-review/2026-09-05/r19-pending-fixed.png)；[连续编辑](docs/ux-review/2026-09-05/r18-edit-fixed.png)；[新应用已评审](docs/ux-review/2026-09-05/r17-contract-fixed.png)。

- R24 从事件时间线追溯决定：系统菜单→事件流→加载更多，能回读本轮 complete 执行记录；浏览器同门只读审计按 kind=confirmation-rejected 查询，核实 `confirmation:c4` seq=648 的 reason 与用户填写逐字一致，proposedBy/decidedBy 都是 mike 的 human 身份。拒绝理由确实落账，缺的是决定后的可读回执，而非数据丢失。时间线当前 18 条中大量 presence/presentation，UI没有查询控件；HTTP已有 kind/domain/rel 参数，能力与人类入口存在差距。

- R23 最终结果（c9971648）：隐藏的 `confirmation:c6` 在首页恢复可见，原始合同目标/原因都匹配本轮停用请求。但批准被拒“目标动作 deprecate 未声明于节点 active”，说明服务层确认依赖未含 Meta 生命周期；纯测试还明确只重放 transition、不产停用伴随事件。**这条更严策略下的 Meta 确认批准路径未修复，不能宣称完整闭环。** 本轮采用既有合同恢复：带理由驳回 c6→重新在人类 Meta 页面两步停用，恢复的 Cedar 默认策略允许直接执行。目录检索 UX0905 为 0/11，inbox 为0；新应用已停用，当前工作目标完成。停用后的旧实体/flow/meta URL返回403 scope_insufficient，而不是 D71 文档声称的404；详情页把该403误说成“服务不可用”，另列 P1诚实回执缺口。
- R25 默认风险策略恢复复验：对本轮文章 post:ux0905 重新发布→两步归档，直接变已归档，不再多出审批请求或未知错误。容器内 policy.cedar SHA256 与仓库均为 b09c30c3fa731762b8fd96965defb4d5792061dc42836a207a3820072162aa25。默认 human允许、agent高风险挂起的矩阵测试通过；未改策略文本，仅补入镜像。

### 第二轮结案表

主故事为 R01、R03–R17、R21–R24（20 条）；R02、R18–R20、R25 为5条回归/延伸。
“完成”表示实际目标有结果，不表示没有体验债；R23的严格策略分支保留失败证据。

| 故事 | 实際目标与结果                                             |
| ---- | ---------------------------------------------------------- |
| R01  | 公网真实 PKCE 登录并恢复工作台                             |
| R02  | 工作区继续捕捉；别名缓存修复后原位切换动作                 |
| R03  | 新想法创建、发展、保存正文，实体/集合回读                  |
| R04  | 无视角起草阻塞复现；显式选择前置引导已部署                 |
| R05  | 当前对象只读评审、字段引用点击回同一事实                   |
| R06  | AI 精确标记当前想法成熟，画布同步                          |
| R07  | AI 高风险归档挂起、人类批准、原对象归档                    |
| R08  | 跨应用工作线创建、搜索挂材料、移出而不删原文               |
| R09  | 工作线暂停、恢复、完成、归档并保留工作集                   |
| R10  | 待办创建带备注、完成、重新打开、精确读回                   |
| R11  | 人类归档取消与继续，识别202回执断点并修复                  |
| R12  | 带理由驳回、业务对象保持不变                               |
| R13  | 发布向导必填错误恢复、分类正文、发布前改名、发布落库       |
| R14  | 新文章下线、重新发布、再次下线，正文保留                   |
| R15  | 不完整应用候选invalid、修订v2 ready、机械diff与checks      |
| R16  | 无视角深链接选择应用、两步激活、新应用立即可见             |
| R17  | 新生应用连续保存、完成/重开；Siren/judge分叉修复后完整运行 |
| R18  | 原应用捕捉、列表同步、实体页连续编辑与最新值预填复验       |
| R19  | 待确认状态与直达链接、确认批准、待办归档复验               |
| R20  | 390px工作线页面，scrollWidth从656降至390                   |
| R21  | 新应用授权展开、刷新授权、退出真正结束SSO、重登            |
| R22  | 历史会话恢复、新会话空白、只读询问得到当前inbox=0          |
| R23  | 新应用停用；旧严格确认批准失败，驳回后默认策略停用成功     |
| R24  | 时间线翻页与同门审计，驳回理由/身份逐字核实                |
| R25  | 恢复既有Cedar策略后，人类两步归档直接完成，不再额外审批    |

### 八类已部署修复与验证

| 修复                                  | 证据                                                                        |
| ------------------------------------- | --------------------------------------------------------------------------- |
| 缓存 canonical rel/别名与飞行旧读失效 | 两个红例转绿；想法和新应用原位更新                                          |
| 同节点保存后动作永久禁用              | 组件红例转绿；实体页二次编辑预填最新值                                      |
| 草稿无应用视角提交后才失败            | scope选择来自授权sitemap；无默认暗选，激活实测                              |
| 固定书桌撑破移动端                    | 390px前后截图，宽度测量                                                     |
| Business/Meta 202未知错误             | 共用解析函数、端到端组件透传、业务待确认链接实测；Meta严格202分支由测试锚定 |
| Siren与judge参数合同分叉              | 同一生成函数；false/true/缺省及额外字段拒绝；新应用状态链实测               |
| Web镜像漏带Cedar策略                  | OCI资产门禁、runtime SHA一致、归档seq814为cedar:policy0                     |
| Meta目标确认项不可见                  | 归属解析复用、授予外负例；原隐藏c6在首页恢复可见并可驳回                    |

本轮代码提交：93239b7f、5c14facb、5cedbb00、256645fe、c9971648。
最终验证：`pnpm check`（4046 passed / 15 skipped，exit0）、`pnpm governance:strict`、相关 Prettier；
home三镜像精确SHA/digest、preflight/up/status；公网 /live、/version=200且release=c9971648；
匿名 /、/meta、/session=307，/api/entity、/api/auth/session=401。
八长期服务healthy，retained volume hash未变。未单独重跑完整Playwright套件；本轮UI行为以上述真实部署浏览器证据为准。

部署文档 `DEPLOYMENT.local.md` 已同步至 home，双方SHA256为
`6c3b6a43669a32743836dd21c9d1a857a9d49ba2b2cb5bdd4c48fd5056d25881`。
代码以Git bundle fast-forward至home；当前未推送origin。

### 本轮数据台账

- idea:ux0905：已归档，内容与记录保留。
- idea:ux0905-2：发展中，第二版连续编辑结论保留，供继续体验。
- todo:ux0905：已归档，备注保留。
- post:ux0905：最终已归档（R25），正文/分类/标签保留。
- thread:ux0905-deep-review：已归档，保留idea:ux0905引用；临时文章引用已移除。
- draft:71cb15c56f5236491e0d：v2已接受；ux0905-review-lab已停用，旧实例因归属停用不可访问。
- confirmation:c3、c5批准；c4、c6带理由驳回；当前inbox=0。
- 本轮业务删除使用声明的归档/移出/应用停用语义，事件日志没有被改写或删除。

### 需要下一轮优先处理的明确限制

各项候选方案、落位与验收见[修复清单 G01–G15](docs/ux-review/2026-09-05/remaining-gaps.md)；方案备注不表示已经实施。

1. Meta高风险动作若在更严策略下进入pending，服务层确认依赖缺生命周期，且纯确认执行不产Meta伴随事件；不能仅补flows让节点变化就宣称应用已停用。需独立设计与服务层回归后再启用该路径。
2. 停用成功后403导致详情页显示“服务不可用”，丢失成功回执；D71文档404与实况403也需收敛。
3. 知情确认卡缺对象名称/变更摘要，决定后缺状态和理由回执；数据完整但人仍付验证成本。
4. 新激活页把缺diff武断归为旧日志；approved之后重新计算的检查与批准时证据混排。

用户对当前未正式使用系统的体验数据增删改查与优先简单修复授权，已写入本地记忆扩展说明；凭证只用于登录，未进入本文或仓库。
