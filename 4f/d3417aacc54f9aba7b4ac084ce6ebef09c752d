T22 Phase I Authentication Safety Gate correction and live evidence (2026-08-24). Root cause: Istio RequestAuthentication verified JWTs but did not forward the original Bearer credential, preventing mandatory application-layer verification. Added forwardOriginalToken=true to static and pure-rendered jwtRule. Automated: 9 focused auth/CLI/Worker/Helm files, 109 tests passed; Helm lint/template with mothership overlay passed. Live K8s release revision 17 after proxy propagation: real ui4a-agent client-credentials token passed sitemap (200); claims asserted issuer/audience/azp and read/write/policy scope while approve=false; missing/malformed/wrong issuer/expired/nbf/signature returned 401, wrong audience returned 403; over-scoped approve request returned 400 and granted no approve scope; both external internal-callback paths returned 404. CLI cleartext HTTP Bearer guard exited 3 before fetch with no token output. Tokens and Secret material stayed only in shell variables and were unset; none were written to files, command evidence, or this note.
