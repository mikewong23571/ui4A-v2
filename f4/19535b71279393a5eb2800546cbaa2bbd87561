Task: 首个消费方接线 + clientView 协议一次性切换\n\nSwitched ClientView to protocol v2 with a closed presence anchor; navigation completion keeps its independent v1 schema. Removed the legacy route-based metaPlaneFromClientRoute path and made Chat choose the plane from the single situation assembler. Entity scope selection now consumes the same assembled situation after trusted identity resolution. Added presence sequence capture to clientView when the reporter receives an append result, updated conversation/chat/API/browser fixtures, and added a consumer-matrix source assertion.\n\nFiles include packages/shared/src/presentation/chat-view.ts(.test), apps/web/src/chat/{client-view,conversation,start}, apps/web/src/app/api/{chat,entity}, apps/web/src/engine/situation*.test.ts, apps/web/src/presence/client.ts, and related protocol fixtures.\nVerification: 8 focused files / 48 tests, web/agent/shared typechecks passed.\nWhy: satisfy GR2 with one current-view shape and make Chat/entity behavior derive from the same structured situation.
