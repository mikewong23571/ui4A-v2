# T61 Review

实现基线：ea3bbe90。根负责集成自审；work_content_word 子任务对完整 diff 的有界复审并非完全独立作者审查。

## 发现与处置

- P2：work-content 忽略组合的 excludedMemberRels。修复为有排除集合时回到通用 repeat，拒绝复用会绕过排除的已存工作区呈现；纯与组合回归通过。
- P2：分组扁平化丢掉分组自己的动作。保留分组实体及其规范到达路径，同时完整保留子责任；组件回归通过。
- P2：材料选择唯一化后丢失 active/approval/event 人类入口。恢复“其他关联”次级合同表单；不在主面铺参数，原动作裁决不变。
- 集成：成功写入导致整面重建关闭多选弹层。加入临时呈现重建 hold，事实失效与 fresh exec 不暂停；弹层内读取当前授权投影，关闭后合并重建。
- 集成：无可读呈现时缺少恢复入口。补齐返回首页；权限失败不泄露目标名称或伪造零材料。
- 集成：参考标签退回内部引用。按出生定义的声明字段和集合声明取可读标签，canonical CVE identity 与独立实体面一致。
- 内容：仅对声明 text/markdown 的字段使用 Markdown；正文标题不制造第二个页面 H1。

## 回归装置修正

切换到规范对象页后等待工作区离场，再与目标表单/讨论输入交互；否则用例会操作尚未卸载的旧视图。URL 验证比较参数值，不约束 query 顺序。新材料弹层验证紧凑行与单项预览，不恢复默认全量材料正文。

没有使用强制点击、延长超时或删除权限/责任/写入断言来通过回归。生产判断继续由合同与服务器承担，原型示例状态没有进入产品。
