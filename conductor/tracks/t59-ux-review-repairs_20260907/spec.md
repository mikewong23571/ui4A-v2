# T59 Spec

用户授权：2026-09-07 在完成体验审查后要求“修复并且部署”。验收来源为 docs/ux-review/2026-09-07/review.md 的 F01–F11 与 R01–R24。基线 HEAD/线上 release 均为 4e018fcb38a780fc6c1c0108786bb5de618e5cc2。

## 目标

恢复真实助手服务；同一工作材料的可读性、导航和更新一致；人能清楚创建、更正、移出和结束工作，不必理解内部标识。代码经测试、固定镜像部署到 home-compose 公网入口，再完成真实浏览器复验。

## 要求

1. F01/F02：消息从权威日志同 principal 解析，工作线成员与详情一致；状态标签来源于声明，保留机器状态；无新业务存储、跨主体隐藏不变。
2. F05/F08：成员行/卡/底部/目录导航保留显式处境；成员变化更新同页所有呈现与观察；提交失败不假成功。
3. F03/F04/F06/F07：明确归档对象与影响，机械细节按需；默认移出选择现有成员；字段错误可定位可恢复；内容面仍由合同驱动，不添加应用特判。
4. F09：配置的真实提供方请求成功，不更换模型或引入 fallback；失败有简洁说明与可恢复入口，历史/现场错误一致保留审计详情。
5. F10/F11：新事项不静默带旧值；待办声明支持内容更正与终态收尾。存量定义更新必须受治理，birth-pinned 实例边界诚实，不绕过 human-only activation。
6. 原用户工作线/材料不删除；用标记样本验证；最终交付固定 SHA、三镜像 digest、健康与公网浏览器证据、同步运维文档 hash。

## 非目标

不改变凭证授权、业务确认/审批责任、不重写日志、不开始 T58 冻结试用、不新增规则智能/业务路由、不做 UI 全站换皮。必要架构条款修订先写 DECISIONS；不能为消除症状走第二状态或旁路写。
