T2 Phase A — Phase Verification & Checkpoint(自治验收,编排 agent 代行)

验证证据(2026-08-21,全部实跑):
1. CI=true pnpm check 全绿:typecheck(web/worker/engine/shared)+ eslint + vitest 108 tests / 14 files。
2. engine 测试 86 用例(≥30 DoD):parse 14 / machine 13 / schema 4 / judge 15 / effects 16 / siren 13 / sitemap 10 / purity 4+1。
3. 两栖性(DoD 2 双保险):purity.test.ts 文本级扫描 10 个库源(无 node: import、无 process/Buffer)+ shell grep 复核 clean;engine tsconfig types:["node"] 仅为测试类型解析服务(说明:types:[] 会整体排除 @types 包致 node:fs 无法解析,与 shared 同口径)。
4. Siren 形状(DoD 3):siren.test.ts 断言 properties/actions/links/guard-results/entities 键名与 arch-brief §2 一致。
5. 三层顺序(DoD 4):judge.test.ts '未声明动作且 guard 也不满足 → layer=undeclared' 等顺序证明用例通过。
6. 影响作用域:git diff 690237c..HEAD 仅 packages/engine/**、packages/shared/**、pnpm-lock.yaml、eslint.config.mjs(+1 行 engine glob)、plan.md——apps/ 与 conductor/ 其他文件未动。
7. 覆盖率:仓库未配置 coverage provider(T1 基座未装 @vitest/coverage-v8,装它需动根 devDeps,超出本任务作用域);以逐模块测试文件映射(每个功能模块均有同名 test)+ 86 用例矩阵替代,覆盖率工具化建议留给编排者决定。

手动验证等效(自治):本 phase 为纯函数库,无可启动 UI;自动化等效 = 上述测试套件实跑。

偏差记录(详见各任务 note):effect 数组化、节点∪动作字段合并校验、FNV-1a 版本短码、append.flow 字段、@types/node devDep。
