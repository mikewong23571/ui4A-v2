Correction: T29 created presence_current lazily via ensurePresenceTables, which fails in production where the runtime role has no DDL privilege (verified: CREATE TABLE IF NOT EXISTS still requires schema CREATE). Migration registry gains version 2 'presence-projection' applying PRESENCE_DDL as the migration role (default privileges grant runtime DML); ensurePresenceTables is a no-op under the production profile. Migration contract suite updated to the v2 registry (14 tests pass). Found by the rev 33 production Playwright run: POST /api/presence returned 503.
