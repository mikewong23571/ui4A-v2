# 2026-09-07 以人为本的产品体验审查

入口：https://ui4a.styleofwong.cn/canvas?focus=thread%3Aux-review-2026&thread=ux-review-2026

> 后续修复已发布为 `0230c8db`；结果见文末“修复与发布”。下列审查结论保留原时点。

## 结论

本轮走查已完成，当前产品体验不通过。共执行并记录 R01–R24，包含成功、带体验缺口完成和受阻三类结果；不是 24 项全部通过。发现 11 项主要问题，其中 P0 1 项、P1 3 项、P2 7 项。

首要问题是人必须替系统查证自身状态：真实消息被报不存在；同一材料不同链接带到不同处境；移出后同页出现两个答案；助手询问当前工作时失败。它们直接增加恢复上下文与验证成本，与保护注意力的产品目标相违。

建议处理顺序：恢复助手请求 → 统一材料的授权解析、导航和更新 → 改善创建、更正、移出与知情确认 → 减少默认机械信息。实现与部署不是本轮交付，本轮没有修改产品代码或发布镜像。

## 审查边界与进度

本轮独立记录，保留 2026-09-05 的既有证据。以恢复工作上下文、理解事实、知情操作、追溯依据、减少确认与验证负担为判据。工作线的 completed 状态不代表本轮审查已完成，更不构成 T58 连续真人试用验收。

证据包括用户截图与文本、Chrome 公网真实登录与业务操作、5 张本轮截图、本地同 SHA 代码只读核查。公网 `/version` 已核实为 `4e018fcb38a780fc6c1c0108786bb5de618e5cc2`，与本地 HEAD 一致。浏览器使用 mike 账户，凭证不写入记录。

## 即时发现

### F01 / P1：消息引用显示“对象不存在”，上下文无法从概览恢复

- 用户截图：`message:11ec6940-696e-4d84-a938-210e01e2e6ee`、`message:9c8fc95d-5a29-425d-b84f-592a08a97629` 均以完整 UUID 代替可读名称，状态为“对象不存在”。
- 用户影响：不知道这两条材料是什么，也不知道是否丢失、权限变化或暂时读取失败；必须离开工作线自行查证。
- 本地代码线索：`packages/engine/src/projection/work-thread.ts` 的成员卡依据业务快照解析；`apps/web/src/chat/message-entity.ts` 则通过 chat-message-appended 事件提供同 principal 的消息详情。概览解析和详情读取是不同路径，尚不能认定消息已丢失。
- 复核结果：R02/R03 已读到两条原文，R22 用新消息再次复现误报；跨身份行为本轮未执行，修复仍必须保持存在性隐藏。
- 验收要求：在当前授权下可读的材料显示声明身份/摘要并可到达原文；无法证明不存在时不得把解析失败当作不存在；不将聊天内容复制进第二权威状态库。

### F02 / P2：状态语言不一致

- 截图中工作线状态为“已完成”，材料“预约体检”的状态为 `archived`。
- 用户影响：需要理解机器状态词，并自己分辨工作线与材料的完成含义。
- 边界：D78 决定 2 明确要求成员携带合同状态原词；不能直接在通用渲染器添加业务状态翻译表。
- 验收要求：状态的机器值保留，面向人的名称由声明提供；未知状态诚实呈现，不按英文关键词推断语义。

### F03 / P2：确认提示缺少明确对象与影响

- 截图在“恢复工作线”后单独显示“需要确认”，下方才是“归档工作线”。
- 用户影响：不清楚提示修饰哪项操作、将改变什么、是否已在等待处理。
- 本地约束：D78.4 记录线程生命周期不经 Cedar 确认门的现状，不能把 UI 两步提示当作服务端确认覆盖的证明。
- 复核结果：R13 已验证预览和取消；最终归档未执行。用户给定工作线未被改变。

### F04 / P2：主阅读面由操作和机器标识占据

- 截图在标题/状态/三行材料之后连续展示合同宣言、添加、手动填写、移出、恢复、确认、归档及重复 rel 链接。
- 用户影响：能看到“可点什么”，但无法从此页确认产出、依据或下一次接续工作的位置。
- 验收要求：默认优先呈现已有的目标、可读材料与责任；技术入口按需可达；没有验收证据时不将 completed 推断成目标达成。

## 原定覆盖与实际边界

本轮实际覆盖见 R01–R24。助手回答和待办内容更正受阻，不计通过；最终不可恢复归档、审批/激活、退出/改密、移动端、精确 200% 缩放、并发重复提交未验证。缩放快捷键被工具拒绝，未形成有效测试，不据此判断产品。Escape 关闭表单与焦点恢复已实测。未跑自动化测试：本轮只改审查文档，不用旧测试数量替代浏览器证据，也不代表 T58 两周真人验收。

## 浏览器即时证据（已登录 mike）

- R01：从指定深链接完成正常登录，回到 exact thread；未丢失 returnTo。中文产品的登录页为英文，记语言一致性债。
- R02：概览第一条消息主行点击可读到“帮我创建一条待办：下周三之前完成季度汇报 PPT”。**F01 确认为误报，消息并未丢失。** 点击后 URL 只剩 focus，无 thread，无“返回本线”。浏览器返回才能恢复线。
- R03：通过页面底部第二条消息链接进入，读到“现在有哪些应用？”，URL 保留 thread，并提供“返回本线”；返回成功。两个入口对同一类对象有不同导航语义，新增 **F05/P1 主行导航丢共同上下文**。材料抽屉也误报不存在，但其链接保留 thread。
- R04：打开“相关材料（3）”，三条工作集可见，各有移出入口；名称/状态重复 F01。截图：[materials.png](materials.png)。未修改用户给定工作线。
- R05：首页当前无待处理/进行中事项，有发起与历史入口；顶栏却写“已选 3 个对象”，未见用户选中操作，易误解成先前选中材料（需明确其来源集合语义）。
- R06：发起工作→创建工作线，只需填写目标；空提交出现 `Errors` / `must have required property '目标'`，补填可恢复。新增 **F06/P2 校验错误直接暴露机器语言**。
- R07：创建“UX0907 体验样本：整理审查发现并验证材料与状态闭环”成功，提交期间按钮禁用；对话框关闭、集合出现一条进行中记录，未自动进入新线，需另点标题。新建 rel=`thread:bc67e5e2-6761-4855-81d9-bb0b81913de3`。
- 部署核验：公网 `/version` 的 gitSha=`4e018fcb38a780fc6c1c0108786bb5de618e5cc2`，buildDate=`2026-09-06T10:47:15Z`；与本地 HEAD 一致。
- R08：新线→添加涉及对象→搜索 UX0905→选“UX0905 连续编辑回归”，主面出现材料，计数变 1。搜索结果把同一旧工作线分别列在“已结束的工作”“我的工作线”两个集合中；同一想法在选择器为“发展中”，挂入后变 `developing`，F02 再现。
- R09：主面的“移出涉及对象”要求选择 `context/active/approval/event` 并手填 rel，说明却让用户去“添加”选择器。**F07/P2 移出表单将机器分类和路径负担交给用户**。Escape 正常取消，焦点回到触发按钮。
- R10：相关材料→逐项移出→抽屉与计数变 0；关抽屉后主面仍有“UX0905 连续编辑回归”及底部链接。**F08/P1 同页移出结果不一致**；截图：[detach-stale.png](detach-stale.png)。不是仅瞬时提交状态：已经读取结果、关闭抽屉后仍残留。点击残留底部链接，原对象正文“第二版：连续编辑不再被已执行状态锁住。”仍可读，证明移出未删除原对象。
- R11：返回本线后残留材料消失。暂停→已暂停/恢复按钮→恢复→进行中，主面即时更新。
- R12：完成→已完成→恢复工作线→进行中闭环成功；无材料、无验收证据也可完成，页面仍只显示生命周期，不提供验收结论。不能把它当作审查成功。
- R13：归档首次点击只展开“已请求…尚未执行”、确认执行与取消。取消后仍为已完成；无对象影响/归档后是否可恢复的说明。截图：[archive-preview.png](archive-preview.png)。未执行最终归档，保留可恢复样本；移出已覆盖关联删除。
- R14：打开助手恢复旧会话而非自动新建；“历史回合当时上下文”可展开，明确原焦点 idea:item-2。当前输入范围显示新工作线 UUID，历史/current 分离但机器标识阅读负担仍高。
- R15：新会话→只读询问当前工作线目标/状态/材料数量，不改业务对象；实际返回 **driver_fail**。失败详情：HTTP 400 / MissingSessionID，提供方要求 `x-opencode-session`。新增 **F09/P0 本轮助手主任务不可用**（一次真实失败，不能外推所有会话故障率）；截图：[assistant-failure.png](assistant-failure.png)。页面没有直接重试，失败区需展开技术详情才能理解原因。没有产生成功答复，AI 共同上下文质量本项受阻，不能记通过。
- R16：应用目录可按“待办”筛到 1/11，名称、用途与入口可见；从工作线顶栏“应用”进入会丢 thread，需用户重新找回工作线。目录中的实验应用名称和说明仍与日常应用并列。
- R17：待办工作区→再记一条→添加待办，表单预填上一次 UX0905 的标题/备注；没有清空成新事项，新增 **F10/P2 新建沿用旧内容、易误提交**。改填本轮标题和备注后成功产生 `todo:ux0907`，工作区列表与捕捉结果同步到新值。
- R18：尝试修改刚建待办备注，列表“更多操作”和详情均只有完成/归档，无编辑入口；目标无法完成，记录 **F11/P2 已建待办不可更正内容**（当前线上应用合同能力缺口；不能在 UI 伪造编辑动作）。
- R19：待办完成后状态变已完成、显示重新打开，备注保留。当前 done 节点无归档，若用户想从已完成状态收尾，必须先重开；同为合同能力缺口，不以旧 T54 交付文字替代 live 行为。
- R20：待办重新打开→进行中→再次完成→整页刷新，仍显示已完成与原备注。测试待办最终保留已完成状态，恢复链路成立。
- R21：系统→我的授权→刷新授权，正常返回原面板；授权信息可见且范围未变。面板主要是 principal UUID、scope 和英文应用键，普通人难以据此判断“我能做什么”，作为 F04 的延伸。
- R22：刷新/重新打开助手后失败回合仍在，但原先折叠的技术错误变成默认对话正文，含重复 provider JSON 和链接；本轮新消息 `message:3ff54798-704c-45d5-ab70-b1d92e0f79e7` 自动成为工作线材料并误报不存在。点击底部引用后完整原文可读，证明 F01 也发生于新数据，见 [new-message-readable.png](new-message-readable.png)。消息挂线属于现行聊天记账行为，本次只读问题没有被执行成其他业务动作。
- R23：在输入框写未发送测试草稿→收起→展开→分栏，草稿与会话保留；验后清空测试草稿，没有发送。真实截图检查当前窗口的主面/助手可读，未声称窄屏通过。
- R24：样本工作线收尾为已完成，点击“创建时的目标原文”，正文与创建输入一致，保留本线并提供返回入口。这个来源阅读路径清晰可用；样本 completed 仅表示体验动作结束，不是缺陷修复通过。

## 优先级与可复核验收

| ID | 级别 | 要解决的用户问题 | 验收要求 |
| --- | --- | --- | --- |
| F09 | P0 | 助手询问当前工作即失败 | 相同真实请求成功、有可点依据；提供方故障有简洁恢复路径；不使用规则助手兜底；历史和现场失败披露一致 |
| F01 | P1 | 消息存在却报不存在 | 新旧同 principal 消息在概览与详情同样可读；不存在/跨主体保持存在性隐藏；不靠补写聊天副本修复 |
| F05 | P1 | 点击材料丢工作线/应用处境 | 主行、底部、材料抽屉使用相同导航上下文，进入与返回均保留用户声明 |
| F08 | P1 | 移出后计数和主面互相矛盾 | 成功后的抽屉、主面、计数、助手观察同步；失败保持原成员；返回/刷新不再是必需补救 |
| F02 | P2 | 中文与机器状态词混用 | 状态名称从合同声明取得，机器值可审计，无按应用写死翻译表 |
| F03 | P2 | 归档确认不解释影响 | 展开后能知道对象、变化、可恢复性；区分本地确认提示与真正待审批项，不谎称服务端门覆盖 |
| F04 | P2 | 概览优先暴露机制而非工作 | 目标、材料、已声明责任优先；UUID/重复链接/合同宣言按需可查，无证据不宣称目标通过 |
| F06 | P2 | 空提交要求读英文校验语句 | 用字段声明提供可定位的缺项提示，保留已填数据并可恢复，机器详情退到诊断 |
| F07 | P2 | 移出需手填分类和路径 | 从现有成员选择、明确仅移出引用，复用声明动作和 fresh read；空工作集不诱导手填 |
| F10 | P2 | 新建意外继承旧内容 | 再记一条清楚表达新事项；默认不会静默复用上次标题/备注；有意复用必须可辨 |
| F11 | P2 | 已建待办内容无法更正 | 在应用定义治理后提供合法编辑及合适终态收尾动作；详情/列表连续编辑与回读一致 |

## 数据与交付台账

- 用户指定 `thread:ux-review-2026`：只读，未恢复、归档或修改其原材料。
- 本轮 `thread:bc67e5e2-6761-4855-81d9-bb0b81913de3`：最终已完成；临时关联 `idea:ux0905-2` 已移出，原想法未改；保留失败回合消息作为本轮证据。
- 本轮 `todo:ux0907`：最终已完成，标题“UX0907 核对审查结论”，第一版备注保留；未修改现有待办。共享捕捉流程经“再记一条/添加待办”正常推进，当前产物为本轮待办。
- 本轮聊天会话前缀 `63dcfa4f`：一次只读问询失败，保留失败历史；未发送测试草稿已清空。
- 本轮修改仅审查记录及截图，旧轮文档追加索引。无代码修复、部署、审批、权限扩大、基础设施或事件日志删除。


## 修复与发布（用户随后授权）

当前公网 release：`0230c8db12f5d8bd0b622e2d4a8018874c2d1b93`。F01–F09通用修复已部署并做公网复验；F10/F11本地定义候选及校验完成，线上仍需人类批准，旧实例不会自动迁移。**不能声称11项全部已在线解决。**

- 消息摘要、原文、声明状态与材料链接一致；长消息末尾及换行可读。
- 材料主行和应用目录保留工作线；移出选择当前授权对象，计数与主面同步；聊天自动挂入消息也即时刷新。
- 归档预览显示真实目标标题与声明影响，明确尚未发送请求；取消不归档。
- 必填错误为“请填写‘目标’”，保留输入与可展开诊断；规则/裸链接按需展开。
- 实际模型恢复：同一deepseek-v4-flash，真实会话请求头经既有代理动态转发。旧错误折叠；修改后再试只恢复原问题，不自动重放动作。
- 最终check：4445 passed/15 skipped；生产构建与strict通过。完整E2E首轮90passed/29skipped/8failed/2not-run，旧文案和状态断言修正后定向重跑全部受影响项，累计覆盖100passed/29skipped。
- 8长期服务healthy，公网状态合同通过；持久卷hash不变。运维文档两端SHA256一致（6579d067…3bac）。

视觉证据：[原工作线修复后](deployed-thread.png)、[长消息全文](deployed-full-message.png)、[移出同步](deployed-detach.png)、[必填错误](deployed-validation.png)、[390px工作线](deployed-thread-mobile.png)、[390px助手](deployed-chat-mobile.png)、[最终知情确认](final-confirmation.png)、[最终聊天材料同步](final-chat-sync.png)。540px放大等效布局无横向溢出；原生200%缩放未获有效工具证据，不计通过。

[待办两份定义候选、影响和适用边界](definition-proposals.md)。最终实时核实均为pending-approval；本轮没有代行人类批准，也没有迁移旧bornVersion。专用复验线e7a2e50b-afe3-4561-ac67-90884e0ed43f已完成并保留1条消息；bc67e5e2样本保留2条消息；原ux-review-2026只读未改。
