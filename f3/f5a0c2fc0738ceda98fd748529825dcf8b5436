Task: Production Agent Runtime activity wiring Green\n\nSummary:\n- Production Agent Run activities now consume canonical runtime default profiles and authenticated Runner transport; local profile retains existing in-process execution.\n- Worker keeps specialization prepare/compile/progress/DB/collect/verify/finalize. Only generic compiled Codex messages/schema/sandbox cross Runner transport.\n- Compiled requests map into the existing RunnerDelivery wire and unwrap RunnerDeliveryResult; no fourth wire format.\n- Host origin/token derive from server profile/deployment Secret plus deployment-owned origin map.\n- Request backend/provider/model/cwd/env overrides fail before compile/transport; selected backend unavailability has zero local/alternate fallback.\n- Canonical profile supplies specialization, so arbitrary activated definition refs are supported.\n\nFiles:\n- apps/worker/src/runtime-backends/production-wiring.ts/.test.ts\n- apps/worker/src/activities.ts\n\nVerification:\n- relevant Worker matrix: 14 files / 142 tests\n- main focused + typecheck rerun passed\n- scoped ESLint/Prettier/diff: pass\n\nBoundary:\n- Runner default daemon production processor/executor/authorizer is a separate next task; until then live /deliver remains unavailable.
