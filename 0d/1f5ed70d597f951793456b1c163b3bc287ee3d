Task: Persistent Runner PKI initializer Green\n\nSummary:\n- Added real pki-init command using fixed OpenSSL execFile argv with shell disabled.\n- Empty retained volume creates one root and two canonical leaf pairs via staging and a final completion marker.\n- Complete valid inventory is reused with zero process calls/writes; partial or invalid inventory fails closed.\n- Validates ownership, 0600/0644 modes, CA/SAN/chain/key/expiry; root key stays root-only while leaf material is owned by edge UID/GID 1000.\n- Runner image installs and verifies OpenSSL.\n\nFiles:\n- apps/agent-runner/src/pki.ts\n- apps/agent-runner/src/pki.test.ts\n- apps/agent-runner/src/main.ts\n- apps/agent-runner/Dockerfile\n\nVerification:\n- Runner + OCI image contract: 5 files / 58 tests\n- Runner typecheck/build: pass\n- built CLI temp-dir smoke: created then reused; six material SHA-256 values unchanged\n- temporary PKI test material removed.
