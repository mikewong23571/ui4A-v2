Task: 失败语义回归测试

Summary:
- Closed Chat mechanical failure reasons to the existing four-code contract.
- Added a loop-exception reason factory and normalized unknown driver codes to driver_fail.
- Proved fact-focus start unavailability performs one sitemap read, one exact entity read, returns no steps, and emits start_entity_unavailable.
- Proved delegated first-entity 404 remains unrecorded and finishes delegation-failed with zero steps.

Files:
- apps/web/src/chat/failure-reason.ts and test
- apps/web/src/app/api/chat/route.ts, route-test-kit.ts, route-ai-first.test.ts
- apps/worker/src/delegation.test.ts

Why:
Context narrowing must not expand or silently change the stable failure contract, and start failure must remain honest without reachability probes.

Verification:
- 3 files / 51 tests passed
- pnpm --filter @ui4a/web typecheck
- pnpm --filter @ui4a/worker typecheck
- pnpm governance
- Autonomous acceptance by the orchestrating agent.

Phase C autonomous verification report

Automated commands:
- NODE_OPTIONS=--no-experimental-webstorage pnpm check
  Result: typecheck/lint/governance passed; 356 test files passed, 6 skipped; 2685 tests passed, 10 skipped.
- CI=true NODE_OPTIONS=--no-experimental-webstorage pnpm e2e invariants
  Result: 4 passed, 2 established skips; I5 replay hash 4c6822cece4b over 53 events / 30 rels.

Scripted manual-equivalent verification:
1. Confirm Chat dispatch passes Situation.scope through Temporal and every worker AgentStep.
2. Confirm worker DriverContext uses the public shared disclosure slicer and retains foreign navigation rels without schemas.
3. Confirm inline sitemap GET count is exactly one, including unavailable-preload behavior.
4. Confirm ghost focus emits start_entity_unavailable with no steps/probes and delegated 404 emits zero-step delegation-failed.
5. Confirm failure codes are exactly no_progress_loop, driver_fail, start_entity_unavailable, loop_exception.

Confirmation:
Autonomously accepted by the orchestrating agent under conductor/workflow.md; all scripted expectations and full phase gates passed.
