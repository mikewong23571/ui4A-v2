Task: 处境装配模块\n\nAdded apps/web/src/engine/situation.ts as the single deterministic assembler. It accepts principal, granted scopes, optional presence, explicit structured parameters, and deployment defaults; selects authorized scope with explicit > presence > default precedence and emits site/scope/thread/focus plus a disclosure slice. Added explicit, presence-assisted, CLI/headless, and ungranted-scope matrix tests.\n\nWhy: prevent chat, entity, prompt, and workstation consumers from independently recomputing context.
