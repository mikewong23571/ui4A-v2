T32 Q4 完成:thread.tsx 的 showRel 由 rel.includes('flow')||text.includes('执行 next(') 改为 rel.startsWith('flow:')(flow-entry.ts 的规范前缀);Badge 加 data-testid=flow-rel-badge;删除只为启发式存在的 text 解构;头注释同步。floating-chat.test.tsx 冻结断言迁移:fixture 同时含文本诱饵帧(rel 无前缀+文本含「执行 next(」)与结构化帧(flow:article-drafting),断言唯一徽章是结构化帧的——文本诱饵不再出徽章。mutation 抽查:临时恢复启发式(恒真分支)→ 27 用例 1 红;还原后 27 passed。验证:CI=true pnpm vitest run apps/web/src/components/chat/floating-chat.test.tsx --project unit。全仓扫描:消息文本启发式零残留(trail.ts 的机器层文本生成与 meta link rel 标签过滤不在口径内)。
