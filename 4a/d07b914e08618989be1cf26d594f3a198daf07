T23 Phase A tasks 1-2(自治验收):建立 scripts/governance 三检查(deps/compat/size,纯 Node)与 run-all;check-deps 实测 36 处跨边界引用,归因登记为 4 条例外(worker→web db 存储复用、worker→web engine 测试耦合、web→worker activities 测试耦合、engine 测试读 web bundle fixture);check-compat 固化 66 个 legacy/compat 标记文件为 pendingRemoval 基线,3 个 GR2 禁用路径保持 Red;check-size --write-baseline 固化 33 文件 + 12 目录超限基线。验证:node scripts/governance/run-all.mjs 默认模式仅 GR2 禁用路径 Red(预期),--strict 因基线非空失败(预期);eslint/prettier 通过。
