T22 authenticated-principal system-seed resolution. Added read-only exact-ref/scope and scoped-list resolvers that only accept active pointers with registeredActor=system:bootstrap and fail closed on conflicting content. Native dispatch resolves requester-personal exact definitions first, blocks fallback for an existing inactive personal version, and falls back only when personal is absent. Catalog and exact Meta projection merge system seeds per scope with personal same-name override. Agent Run principal/policy scope remain the requester. Bootstrap event/schema behavior is unchanged. Verification: focused DB/catalog/native/authoring tests 26/26; @ui4a/web typecheck; Prettier; diff check.
